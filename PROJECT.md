# Project Name

## Goal

Describe the actual product, screen, or flow to build.

## Audience

Who uses it, how often, and under what pressure?

## Design References

List files or links in `references/`.

## Required Screens

- Screen 1
- Screen 2
- Screen 3

## Design Parameters

- Brand:
- Primary action:
- Density:
- Navigation:
- Data complexity:
- Mobile priority:
- Accessibility notes:

## Harness References

This project is self-contained after creation:

- `AGENTS.md` contains the agent instructions for this project.
- `design-system/` contains the design system snapshot to follow.
- `design-parameters.json` contains this project's overrides.
- `code/color-tokens.md` points to the local project design system.

When the parent harness improves, intentionally copy the relevant updates into this project's `design-system/` instead of silently changing the project midstream.

## Parent Design System Overrides

Document only the changes that intentionally differ from `design-system/`.

## Done Criteria

- App runs locally.
- Build succeeds.
- Desktop layout checked.
- Mobile layout checked.
- Text does not overlap.
- Design references have been considered.
